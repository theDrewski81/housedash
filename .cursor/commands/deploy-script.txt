sudo git pull
sudo docker compose up -d postgres
sudo docker compose build --no-cache migrate
sudo docker compose --profile tools run --rm migrate
sudo docker compose build app
sudo docker compose up -d --force-recreate app
